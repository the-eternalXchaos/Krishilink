import 'package:flutter/widgets.dart';

class RoleBasedAppBar extends StatelessWidget {
  const RoleBasedAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}